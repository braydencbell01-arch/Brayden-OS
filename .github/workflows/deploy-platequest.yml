name: Deploy PlateQuest

# PlateQuest is its own product and GitHub Pages site — never publish into
# BrayStats (Brayden-OS root) or Jersey Deals (/jerseydeals/ or jerseydeals.online).
# Live: https://braydenbell.github.io/PlateQuest/

on:
  push:
    branches:
      - Brayden-OS
    paths:
      - 'platequest/**'
      - '.github/workflows/deploy-platequest.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: platequest-deploy
  cancel-in-progress: false

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Resolve BraydenBell deploy token
        id: token
        env:
          PQ: ${{ secrets.PLATEQUEST_DEPLOY_TOKEN }}
          JD: ${{ secrets.JERSEYDEALS_DEPLOY_TOKEN }}
        run: |
          if [ -n "$PQ" ]; then
            echo "which=pq" >> "$GITHUB_OUTPUT"
            echo "Using PLATEQUEST_DEPLOY_TOKEN."
          elif [ -n "$JD" ]; then
            echo "which=jd" >> "$GITHUB_OUTPUT"
            echo "Using JERSEYDEALS_DEPLOY_TOKEN (BraydenBell PAT fallback)."
          else
            echo "ERROR: Set PLATEQUEST_DEPLOY_TOKEN or JERSEYDEALS_DEPLOY_TOKEN." >&2
            exit 1
          fi

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: npm
          cache-dependency-path: platequest/package-lock.json

      - name: Ensure BraydenBell/PlateQuest repo exists
        env:
          GH_TOKEN: ${{ steps.token.outputs.which == 'pq' && secrets.PLATEQUEST_DEPLOY_TOKEN || secrets.JERSEYDEALS_DEPLOY_TOKEN }}
        run: |
          set -euo pipefail
          if gh api "repos/BraydenBell/PlateQuest" --silent 2>/dev/null; then
            echo "BraydenBell/PlateQuest already exists."
            exit 0
          fi
          echo "Creating BraydenBell/PlateQuest…"
          gh repo create BraydenBell/PlateQuest \
            --public \
            --description "PlateQuest — license plate spotting, identification, and games" \
            --disable-issues \
            --disable-wiki
          # Project Pages need the repo to exist; first push to gh-pages enables the site.

      - name: Build PlateQuest (project Pages base /PlateQuest/)
        working-directory: platequest
        env:
          PLATEQUEST_BASE: /PlateQuest/
        run: |
          npm ci
          npm run build
          cp dist/index.html dist/404.html
          touch dist/.nojekyll
          if ! grep -q 'PlateQuest' dist/index.html; then
            echo "ERROR: dist/index.html is not PlateQuest" >&2
            exit 1
          fi

      - name: Deploy to BraydenBell/PlateQuest Pages
        uses: peaceiris/actions-gh-pages@v4
        with:
          personal_token: ${{ steps.token.outputs.which == 'pq' && secrets.PLATEQUEST_DEPLOY_TOKEN || secrets.JERSEYDEALS_DEPLOY_TOKEN }}
          external_repository: BraydenBell/PlateQuest
          publish_dir: ./platequest/dist
          publish_branch: gh-pages
          force_orphan: true
          enable_jekyll: false

      - name: Enable GitHub Pages on BraydenBell/PlateQuest
        env:
          GH_TOKEN: ${{ steps.token.outputs.which == 'pq' && secrets.PLATEQUEST_DEPLOY_TOKEN || secrets.JERSEYDEALS_DEPLOY_TOKEN }}
        run: |
          set -euo pipefail
          if gh api "repos/BraydenBell/PlateQuest/pages" --silent 2>/dev/null; then
            echo "Pages already configured."
            exit 0
          fi
          gh api -X POST "repos/BraydenBell/PlateQuest/pages" \
            -f build_type=legacy \
            -f 'source[branch]=gh-pages' \
            -f 'source[path]=/' \
            || true

      - name: Verify live PlateQuest URL
        run: |
          set -euo pipefail
          url="https://braydenbell.github.io/PlateQuest/"
          echo "Waiting for $url …"
          deadline=$((SECONDS + 300))
          while (( SECONDS < deadline )); do
            code=$(curl -sL -o /tmp/pq-live.html -w "%{http_code}" "$url" || true)
            if [[ "$code" == "200" ]] && grep -q 'PlateQuest' /tmp/pq-live.html; then
              echo "Live: $url"
              exit 0
            fi
            sleep 15
          done
          echo "Deploy pushed; Pages may still be propagating: $url"
          exit 0
